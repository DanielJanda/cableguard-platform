# Public deployment placeholders – copy to domains.local.env (gitignored)
# Used by setup scripts and operator runbooks. No secrets.

# Registered domain (replace example.com)
CABLEGUARD_DOMAIN=example.com

# Egress public IPv4 observed on MediaMTX PC (verify against router WAN IP)
CABLEGUARD_PUBLIC_IPV4=194.12.35.108

# Published Lovable frontend origin (for MediaMTX webrtcAllowOrigins + Platform CORS)
LOVABLE_PUBLISHED_ORIGIN=https://98d9308a-3156-47c0-9d35-204618f8df3b.lovable.app

# MediaMTX PC LAN address (port forward target)
MEDIAMTX_LAN_IP=10.6.1.40
